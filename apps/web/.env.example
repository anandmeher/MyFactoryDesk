# API base URL (matches apps/api default port from `pnpm dev`)
VITE_API_URL=http://localhost:3030/api/v1

# Shown in the footer; defaults are baked in if these are unset
VITE_APP_VERSION=0.0.0
VITE_SUPPORT_PHONE=+91 9999999999
